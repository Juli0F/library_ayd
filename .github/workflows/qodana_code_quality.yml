name: CI/CD Spring

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

#  build-and-test:
#    runs-on: ubuntu-latest
#
#    steps:
#      - uses: actions/checkout@v3
#      - name: Set up JDK 17
#        uses: actions/setup-java@v3
#        with:
#          java-version: '17'
#          distribution: 'adopt'
#
#      - name: Build with Maven
#        run: mvn clean package
#
#      - name: Run Tests
#        run: mvn test
#        env:
#          SPRING_PROFILES_ACTIVE: test

  deploy-prod:
#    needs: [build-and-test]
    runs-on: ubuntu-latest
#    if: github.event_name == 'push'

    steps:
      - uses: actions/checkout@v3

      - name: Set JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'adopt'

      - name: Build with Maven
        env:
          SPRING_PROFILES_ACTIVE: prod
        run: mvn clean install -DskipTests.

      - name: test
        run: |
          scp -o StrictHostKeyChecking=no -i private_key.pem ./target/library-0.0.1-SNAPSHOT.jar ec2-user@${{ secrets.IP_VPC }}:/home/ec2-user/
          ssh -o StrictHostKeyChecking=no -i private_key.pem ec2-user@${{ secrets.IP_VPC }} 'java -jar /home/ec2-user/library-0.0.1-SNAPSHOT.jar'
